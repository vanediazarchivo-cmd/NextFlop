_format_version: "3.0"

plugins:
  - name: cors
    config:
      origins:
        - http://localhost:3000
        - http://localhost
        - "*"
      methods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
      headers:
        - Authorization
        - Content-Type
      exposed_headers:
        - Authorization
      credentials: true
      max_age: 3600

services:
  # ============================================
  # AUTH SERVICE - User authentication, profiles
  # ============================================
  - name: auth-service
    url: http://auth-service:3000
    routes:
      # Auth endpoints: /api/auth/*
      - name: auth-routes
        paths:
          - /api/auth
          - /auth
        strip_path: true
      - name: auth-routes-plain
        paths:
          - /auth
        strip_path: false
      # Profiles endpoints: /api/profiles/*
      - name: profiles-routes
        paths:
          - /api/profiles
          - /profiles
        strip_path: true
      - name: profiles-routes-plain
        paths:
          - /profiles
        strip_path: false

  # ============================================
  # SUBSCRIPTIONS SERVICE - Plans & Subscriptions
  # ============================================
  - name: subscriptions-service
    url: http://subscriptions-service:3000
    routes:
      # Subscription plans: /api/subscription-plans/*
      - name: subscription-plans-routes
        paths:
          - /api/subscription-plans
          - /subscription-plans
          - /api/plans
          - /plans
        strip_path: true
      - name: subscription-plans-routes-plain
        paths:
          - /plans
        strip_path: false
      # User subscriptions: /api/subscriptions/*
      - name: subscriptions-routes
        paths:
          - /api/subscriptions
          - /subscriptions
        strip_path: true
      - name: subscriptions-routes-plain
        paths:
          - /subscriptions
        strip_path: false

  # ============================================
  # BILLING SERVICE - Payments, billing
  # ============================================
  - name: billing-service
    url: http://billing-service:3000
    routes:
      # Payment endpoints: /api/payments/*
      - name: payments-routes
        paths:
          - /api/payments
          - /payments
        strip_path: true
      - name: payments-routes-plain
        paths:
          - /payments
        strip_path: false
      # Payment methods: /api/payment-methods/*
      - name: payment-methods-routes
        paths:
          - /api/payment-methods
          - /payment-methods
        strip_path: true
      - name: payment-methods-routes-plain
        paths:
          - /payment-methods
        strip_path: false
      # Billing endpoints: /api/billing/*
      - name: billing-routes
        paths:
          - /api/billing
          - /billing
        strip_path: true
      - name: billing-routes-plain
        paths:
          - /billing
        strip_path: false

  # ============================================
  # MEDIA SERVICE - Catalog, search, media
  # ============================================
  - name: media-service
    url: http://media-service:3000
    routes:
      # Media endpoints: /api/media/*
      - name: media-routes
        paths:
          - /api/media
          - /media
        strip_path: true
      - name: media-routes-plain
        paths:
          - /media
        strip_path: false
      - name: media-static-uploads
        paths:
          - /uploads
        strip_path: false

  # ============================================
  # AUTH SERVICE - Onboarding (nested in auth)
  # ============================================
  - name: onboarding-service
    url: http://auth-service:3000
    routes:
      # Onboarding endpoints: /api/onboarding/*
      - name: onboarding-routes
        paths:
          - /api/onboarding
        strip_path: true
